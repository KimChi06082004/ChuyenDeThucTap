# Cấu hình server
PORT=8080
NODE_ENV=production

# Database connection (sửa lại đúng theo host thật)
DB_HOST=containers-us-west-123.railway.app
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=websitedaythem

# JWT & bảo mật
JWT_SECRET=supersecret

# Cấu hình CORS
CORS_ORIGIN=https://daythem.vercel.app
# Nếu bạn dùng URL MySQL trực tiếp (Render/Railway)
DB_URL=mysql://root:yourpassword@containers-us-west-123.railway.app:3306/websitedaythem
